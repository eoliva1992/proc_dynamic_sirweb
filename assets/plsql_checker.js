// PLACEHOLDER — Este archivo debe ser generado antes de ejecutar la app.
//
// Ejecuta:  cd tools/plsql_parser && ./build.ps1
//
// El script descarga las gramáticas PL/SQL de grammars-v4, genera el parser
// con ANTLR4 y crea el bundle minificado con rollup + terser.
//
// Hasta que exista el bundle real, el editor usa el checker manual de Dart
// (_editor_plsql_checker.dart) como fallback automático.
