name: proc_dynamic_sirweb
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  mobx: ^2.3.0
  flutter_mobx: ^2.2.0
  flutter_js: 0.8.7
  flutter_monaco: 3.4.2
  webview_flutter_windows: ^1.1.1
  shared_preferences: ^2.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.0
  mobx_codegen: ^2.6.0

flutter:
  uses-material-design: true
  assets:
    # Bundle ANTLR4 PL/SQL — generar con: cd tools/plsql_parser && ./build.ps1
    - assets/plsql_checker.js
